
Reduce
======

    (arr, axis)

        output dtype = input dtype
            ss*, nansum, nanmin, nanmax

        int --> float64
            median*, nanmedian*, nanmean

        output dtype = intp
            nanargmin*, nanargmax*

        output dtype = bool
            anynan*, allnan*

    (arr, axis, int)

        int --> float64
            nanstd, nanvar

Nonreduce
=========

    (arr, double, double)

        replace

Nonreduce (axis)
================

    (arr, axis)

        output dtype = float64
            rankdata*, nanrankdata*

    (arr, axis, int)

        partsort, argpartsort

Move
====

    (arr, int window, int axis)

        output dtype = input dtype
            sum, nansum, min*, max*, nanmin*, nanmax*

        int --> float64
            mean, nanmean, median*

    (arr, int window, int axis, int ddof)

        int --> float64
            std, nanstd

* means TODO


Misc
====

- after porting all functions, remove unused imports in pyx files
- port docstrings from old bottleneck
- get function reference in sphinx doc working
- can nogil be used?
