
Reduce
======

    (arr, axis)

        output dtype = input dtype
            ss*, nansum, nanmin, nanmax

        int --> float64
            median*, nanmedian*, nanmean

        output dtype = float64
            rankdata*, nanrankdata*

        output dtype = intp
            nanargmin*, nanargmax*

        output dtype = bool
            anynan*, allnan*

    (arr, axis, int)

        int --> float64
            nanstd, nanvar

Nonreduce
=========

    (arr, axis, int)

        partsort*, argpartsort*

    (arr, scalar, scalar)

        replace*

Move
====

    (arr, int window, int axis)

        output dtype = input dtype
            sum*, nansum*, min*, max*, nanmin*, nanmax*

        int --> float64
            mean*, nanmean*, median*

    (arr, int window, int axis, int ddof)

        int --> float64
            std*, nanstd*

Weird
=====

    (2d arr, 1d arr, int axis)

        nn*

* means TODO
