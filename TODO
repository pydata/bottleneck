
Reduce (arr, axis)

    output dtype = input dtype
        ss*, nansum, nanmin*, nanmax*

    int --> float64
        median*, nanmedian*, nanmean

    output dtype = float64
        rankdata*, nanrankdata*

    output dtype = intp
        nanargmin*, nanargmax*

    output dtype = bool
        anynan*, allnan*

Reduce (arr, axis, int)

    int --> float64
        nanstd, nanvar*

Nonreduce (arr, axis, int)

    partsort*, argpartsort*

Nonreduce (arr, scalar, scalar)

    replace*

Weird (2d arr, 1d arr, int axis)

    nn*

* means TODO
